--- a/skills/test-generator/SKILL.md
+++ b/skills/test-generator/SKILL.md
@@ -1,24 +1,19 @@
 ---
 name: test-generator
 description: plan-implement のフェーズ2で TDD 対象スライスの実装前に内部起動される。失敗するテスト (RED) を書く generator。
-user-invocable: false
-context: fork
-agent: general-purpose
-background: false
-metadata:
-  purpose: produce
-  trigger: internal
-  shape: forked
-  role: generator
 ---
 
 # test-generator
+
+## Codex 実行境界
+
+最初に `../../references/codex-runtime.md` を全文読む。このスキルは独立コンテキストでだけ実行する。入力に `CRYSTALLIZE_CODEX_ROLE=test-generator` マーカーが無ければ、履歴を渡さない新規サブエージェントを起動し、同マーカー、この SKILL.md の絶対パス、project_dir、scoped_payload、output_contract、禁止入力を渡す。子の完了後、親はプロダクションコードの変更がゼロであることと RED ログを自分で確認する。独立実行を利用できない場合は同一コンテキストへフォールバックせず停止する。
 
 spec の受け入れ基準 (テストケース表) から **失敗するテスト (RED) だけ** を書く generator。実装は書かない — GREEN にするのは `code-generator` の仕事で、書く者と通す者を別コンテキストに分けることで「今のコードが通るようにテストを書く」報酬ハックを構造的に断つ。
 
 ## Input / Output
 
-- **Input**: `$ARGUMENTS` = 呼び出し元 (plan-implement) が整形したテキスト — スライスの計画文 / 関連する要件 / **spec の「受け入れ基準 (テストケース表)」の該当行 (AC-n × seam × ケース × 期待結果) と「テストしないと決めたもの」** / テストの実行コマンド / (差し戻しのときのみ) 書き換える対象のテスト一覧とその根拠 (テスト自体の欠陥の指摘、または改訂後の spec の行)
+- **Input**: 呼び出し元 (plan-implement) が委任メッセージに整形したテキスト — スライスの計画文 / 関連する要件 / **spec の「受け入れ基準 (テストケース表)」の該当行 (AC-n × seam × ケース × 期待結果) と「テストしないと決めたもの」** / テストの実行コマンド / (差し戻しのときのみ) 書き換える対象のテスト一覧とその根拠 (テスト自体の欠陥の指摘、または改訂後の spec の行)
 - **Output**: テストファイル (作業ツリーへ直接書く) + 最終報告 — RED の実行証拠 (失敗ログをそのまま貼る。別ファイルに保存しない) と「AC-n × seam × ケース × テストファイル」の対応表
 
 ## 最重要禁則
@@ -30,7 +25,7 @@
 
 ## 手順
 
-1. `tdd` スキルを発動し、その規律 (良いテストの条件・アンチパターン・同値クラス代表1点 + 境界) に従う。ただし tdd が言う seam と境界ケースの合意は `spec` 段階で済んでおり、渡された表がその記録 — この fork で取り直さない (fork は対話できない)
+1. `../tdd/SKILL.md` を読み、その規律 (良いテストの条件・アンチパターン・同値クラス代表1点 + 境界) に従う。ただし tdd が言う seam と境界ケースの合意は `spec` 段階で済んでおり、渡された表がその記録 — この独立子で取り直さない
 2. 既存テストの慣習を確認する — 配置場所・命名・ランナー・ヘルパの流儀に合わせる
 3. 受け入れ基準の表の行を 1 行 1 テストで書く。期待値は表の「期待結果」列と、独立した情報源 (spec の要件・仕様の実例・手で解いた値) から取る
 4. テストを実行して **全件が失敗する** ことを確認する (失敗ログは最終報告に貼る)
