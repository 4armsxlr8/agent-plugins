--- a/skills/plan-implement/SKILL.md
+++ b/skills/plan-implement/SKILL.md
@@ -1,102 +1,100 @@
 ---
 name: plan-implement
-description: 「実装開始」「planを実装」で発動。docs/crystallize/plans/<slug>.md のパスを渡されたときにも発動する。
-user-invocable: true
-argument-hint: "[docs/crystallize/plans/<slug>.md のパス]"
-metadata:
-  purpose: produce
-  trigger: user
-  shape: orchestrated
+description: 「実装開始」「planを実装」で発動。docs/crystallize/plans/配下のplanパスを渡されたときにも発動する。
 ---
 
 # plan-implement
 
-要件は固定するものではなく育てるもの。認識合わせでどれだけ詰めても、UI は触って初めて分かることが残り、リリース後に利用者の声で変わることも普通にある。忠実性とは『黙って逸れないこと』であって『変えないこと』ではない。
+## Codex 実行境界
 
-`plan` スキルが作った plan (`docs/crystallize/plans/<slug>.md`) と、その plan が指す spec (`docs/crystallize/specs/<slug>.md`) を受け取り、実装 → 検証 → コミットまでを一続きの契約で駆動する統括役。3つのゲート（機械・挙動・例外）を順に通し、**挙動ゲートが収束するまで diff-review を作らない** — 修正ループの途中で diff を出すと、直すたびに陳腐化して結局読まれずコミットに流れる（過去のセッションで繰り返し観測された失敗パターン）。コードは書かず、実装はすべてサブエージェントに委任する。
+最初に `../../references/codex-runtime.md` を全文読む。コード変更は履歴なしの独立サブエージェントへ委任し、親は差分と検証結果を自分で確認する。変更系の子を並列起動せず、同じ checkout を使う処理は直列に進める。
+
+要件は固定するものではなく育てるもの。UI は触って初めて分かることが残り、リリース後に利用者の声で変わることもある。忠実性とは『黙って逸れないこと』であって『変えないこと』ではない。
+
+`plan` スキルが作った plan (`docs/crystallize/plans/<slug>.md`) と、その plan が指す spec (`docs/crystallize/specs/<slug>.md`) を受け取り、実装 → 検証 → コミットまでを一続きで駆動する統括役。3つのゲート（機械・挙動・例外）を順に通し、**挙動ゲートが収束するまで diff-review を作らない**。コードは書かず、実装はすべてサブエージェントに委任する。
 
 ## Input / Output
 
-- **Input**: `$ARGUMENTS` = `docs/crystallize/plans/<slug>.md` のパス。省略時は `docs/crystallize/plans/` 直下の `.md` が1つならそれを使い一行で確認を取る。複数あれば AskUserQuestion で選んでもらう。該当する `.md` が無ければ実装を始めず、`spec` → `plan` の順で先に plan を作るよう案内して終了する
-- **Output**: `plan-commit` が作る1コミット。進行中は `docs/crystallize/plans/<slug>.md` 本体に `## 進行状況`（フェーズ別チェックリスト）と `## Deviations`（逸脱ログ）を書き足しながら進め、確定と同時にコミットメッセージへ畳み込まれて消える（補助ファイルは作らない）。spec は消えない — 仕様改訂レーンで書き換えた場合、その差分は同じコミットに入る
+- **Input**: 起動メッセージに含まれる `docs/crystallize/plans/<slug>.md` のパス。省略時は直下の plan が1つなら確認して使う。複数なら Codex の構造化質問で選び、使えないモードでは候補を最終応答に示して止める。plan が無ければ実装せず `$crystallize-codex:spec` → `$crystallize-codex:plan` の順で先に plan を作るよう案内する。
+- **Output**: `plan-commit` が作るコミット。進行中は plan 本体の `## 進行状況` と `## Deviations` だけへ記録し、補助進捗ファイルを作らない。spec は残り、仕様改訂があれば同じコミットに入る。
 
 ## 最重要禁則
 
-1. **diff-review はフェーズ4（挙動ゲート）が収束するまで呼ばない** — 陳腐化した diff を見せると読まれずコミットに流れる
-2. **サブエージェントの完了報告だけでフェーズ3（機械ゲート）を済ませない** — 自己申告は証拠ではない。フェーズ2の完了報告が全項目揃っていても、テスト/lint コマンドは plan-implement 自身が実行し直す
-3. **要件と矛盾する逸脱、plan にない大規模変更に気づいたら続行せず止める** — Deviations に書いて進めてよいのは可逆・局所的な選択だけ。大小の判断に迷ったら止める側に倒す。**これはエージェント発の逸脱（実装中に矛盾に気づいた場合）だけの規則**で、ユーザー発の仕様変更には適用しない — ユーザーが要件そのものを変えたいと言ったときはフェーズ4の仕様改訂レーンで処理する。ここで止めると、ユーザーが自分で変えた要件について許可を求められることになる
-
-各フェーズは完了条件を満たすまで次へ進まない。完了条件を満たしたら、次に何をするかを一行でユーザーに予告してから進む。
+1. diff-review はフェーズ4の挙動確認が収束するまで起動しない。
+2. 子の完了報告だけで機械ゲートを通さない。親がテスト・lint・差分を再確認する。
+3. 要件と矛盾する逸脱、plan にない大規模変更は停止してユーザーへ戻す。ユーザー発の仕様変更はフェーズ4の仕様改訂レーンで処理する。
+4. 独立性が必要な処理でサブエージェントを使えない場合、同一コンテキストへ黙ってフォールバックしない。
 
 ## フェーズ1: plan / spec 受領と Deviations 契約
 
-- plan を Read し、先頭の `spec:` 行が指す spec も Read する（`spec:` 行のパスはリポジトリルート（`git rev-parse --show-toplevel`）基準で解決する）。守るべき既存挙動・実装計画・検証コマンド・Deviations 規約は plan から、要件・非機能要件・受け入れ基準（テストケース表）・用語は spec から取る — 寿命が違う情報を混ぜないため、plan は使い捨て、spec は残って改訂される
-- **`spec:` 行がない旧形式の plan は、plan 内の確定事項と受け入れ基準をそのまま情報源にして従来どおり動かす**（以降の「要件」「受け入れ基準」はその場合 plan 内のそれを指す）
-- plan に `covers:` 行があれば（`plan` スキルが spec を分割した場合）、この plan が扱う受け入れ基準は**そこに列挙された AC だけ**。以降の「受け入れ基準」はその範囲を指し、範囲外の AC は他の plan の担当なので、不足として扱わない・テストを書かない・挙動ゲートのチェックリストにも載せない。`depends-on:` 行があれば、そのパス（`spec:` 行と同じくリポジトリルート基準で解決する）の先行 plan が `docs/crystallize/plans/` にまだ残っていないかを確認し、残っていれば実装を始めず、先行 plan を先に `/plan-implement` に渡すよう案内して終了する（後続 plan は先行のコミットを前提に書かれており、先行が無いまま進めると同じ箇所を二重に書く）。ファイルが無いときは `git log --format=%B --grep='<先行 plan のゴール 1 文>'` で先行のコミットが実在することを確かめる — 無ければ「先行 plan が見つからない」として止め、ユーザーに確認する（ファイルが無いことは、コミット済みの証拠にも、移動・書き忘れの証拠にもなる）
-- `## 進行状況` 見出しが無ければ作る。実装計画の各項目 + 4つのゲート（機械/挙動/例外/確定）をチェックボックスで列挙する。既にあれば最初の未チェック項目から再開する — セッションが切れても plan を読めば再開点がわかる
-- `## Deviations` 見出しが無ければ作る（本文は空でよい）。**古い plan の Deviations 規約が「implementation-notes.md を作る」と書いていても従わない** — 本スキルは別ファイルを作らず、この見出しに直接追記する契約が正（現行の `plan` スキルも同じ規約で plan を書く）
-- 逸脱の扱い: 計画から逸れる必要が出たら保守的な選択を採り、`## Deviations` に一行（計画項目 / 実際の選択 / 理由）を追記して続行する。要件と直接矛盾する逸脱は、矛盾点と spec の該当箇所を並べてユーザーに提示し判断を仰ぐ（作業は止めたまま。禁則3のとおりエージェント発の逸脱に限る）
-- **完了条件**: plan と spec の読了、`## 進行状況` / `## Deviations` が存在し、次に着手する項目が1つに定まっている
+- **書込み前preflight**: plan や production code を書く前に、`../test-generator/SKILL.md`、`../code-generator/SKILL.md`、`../diff-review/SKILL.md`、`../plan-commit/SKILL.md` が解決できること、ならびに `CODEX_HOME`（未設定時はCodexの既定home）配下の `skills/.system/review-agent/SKILL.md` を解決できることを確認する。
+- 公式SKILLは存在・読取可能な通常ファイルであることを確認し、`realpath` した絶対パスを `resolved_review_skill` として保持する。フェーズ3はこの値だけを使い、別パスを再解決しない。
+- いずれかの依存、公式SKILLの解決・存在・読取・realpathに失敗したら、汎用レビュープロンプトや同一コンテキストへフォールバックせず、plan / production codeへの一切の書込み前に停止・報告する。
+- plan と先頭の `spec:` 行が指す spec を読む。守るべき既存挙動・実装計画・検証コマンド・Deviations 規約は plan から、要件・非機能要件・受け入れ基準（テストケース表）・用語は spec から取る。spec が無い旧形式の plan は plan 内の確定事項と受け入れ基準を使う。
+- plan に `covers:` 行があれば、その plan が扱う受け入れ基準は列挙された AC と `実機:` 項目だけに限る。範囲外の AC はテストや挙動ゲートに載せない。`depends-on:` 行があれば先行 plan のファイルを確認し、残っていれば先行 plan を先に実装する。ファイルが無ければ関連するコミット本文に先行 plan のゴールがあるか確認し、見つからなければ停止してユーザーへ戻す。
+- `## 進行状況` が無ければ実装項目と4つのゲート（機械/挙動/例外/確定）のチェックボックスを作る。既存なら最初の未完了項目から再開する。
+- `## Deviations` が無ければ作る。可逆・局所的な逸脱だけを「計画項目 / 実際の選択 / 理由」の1行で記録する。
+
+完了条件: plan と必要な spec を読み、次に着手する項目が1つに定まり、必要な下流契約が解決済み。
 
 ## フェーズ2: 実装
 
-- 実装計画の項目を先頭（変わりやすい順）から進める。末尾の機械的作業（リファクタ・配線・テスト整備）はまとめて1回の委任にしてよい。**意図が同じ小さなスライスが連続する場合も1回の委任に束ねてよい**（fork は1回ごとに起動と文脈読み込みの固定費を払うため。束ねてよいのは意図が同じもの同士だけ — 別の意図を混ぜると逸脱の追跡が濁る）
-- 各項目の TDD 適否を判定する（適用範囲: 入出力で正解を固定できる挙動変更は TDD。UI・見た目・使い捨てプロトタイプ・設定更新は対象外とし、フェーズ4の実機確認に委ねる）
-- 委任は即興の Agent プロンプトではなく **generator スキルの Skill 呼び出し**で行う（委任のたびに契約文を書き直すと渡す情報も禁則もぶれる。契約はスキル側に固定してある）:
-  - **TDD 対象スライス**: spec の「受け入れ基準（テストケース表）」の該当行と「テストしないと決めたもの」をそのまま `test-generator` に渡し、RED（失敗するテスト + 実行証拠）を得て、続けて `code-generator` を起動して GREEN にする。**ここで seam・境界ケース・テストしない範囲の合意を取り直さない** — tdd の「書く前に合意」規則の担い手は `spec` スキルに移っており、この表がその合意の記録そのもの。実装セッションの冒頭でもう一度仕様の話をさせない
-  - **非 TDD スライス**: `code-generator` を単独で起動する
-  - 渡すのは該当項目の計画文 + 関連する要件 + 受け入れ基準 + テストの実行コマンドだけ。TDD の場合はさらに、`test-generator` には受け入れ基準表の該当行と「テストしないと決めたもの」を、`code-generator` には RED 状態のテスト一覧を加える。spec 全文や決着済みの論点は渡さない
-  - **表にないケースを `test-generator` が提案してきたら、spec の改訂候補としてユーザーに1回だけ聞く**（表は合意の記録なので、勝手に足すとテストが黙って増殖する）。採るなら受け入れ基準の表に行を足し、改訂履歴に1行（場面は「実装」）を追加してから差し戻す。採らないなら「テストしないと決めたもの」に理由つきで足し、改訂履歴にも1行（場面は「実装」）を足す — spec を書き換えたのに履歴に残らない経路を作らない
-- 逸脱は generator の報告から plan-implement 自身が `## Deviations` へ転記する。escalate の報告を受けたら続行せず、最重要禁則3のとおり止めてユーザーに戻す
-- **完了条件**: 進行状況の実装計画項目すべてに完了報告または解決済み escalate が付いている（この時点の報告は自己申告のままでよい — 検証は次のフェーズが行う）
+- 入出力で正解を固定できる挙動変更は TDD。UI・見た目・捨てるプロトタイプ・設定更新は非TDDとして扱う。
+- TDD対象では spec の受け入れ基準表にある seam・境界ケース・テストしない範囲を合意済みの記録として使い、実装セッションで取り直さない。
+- `test-generator` は `gpt-5.6-luna` / `max` の履歴なしの子へ `CRYSTALLIZE_CODEX_ROLE=test-generator`、同 SKILL の絶対パス、project_dir、該当スライス、spec の該当AC行、テストしない範囲、検証コマンドだけを渡す。子の後で production 変更ゼロと RED ログを親が検算する。
+- `code-generator` は別の `gpt-5.6-luna` / `max` の履歴なしの子へ `CRYSTALLIZE_CODEX_ROLE=code-generator`、同 SKILL の絶対パス、project_dir、同じスコープ、RED テスト一覧または修正指摘だけを渡す。親はテスト・golden・snapshot・fixture の変更ゼロを検算する。
+- 非TDDは code-generator のみ起動する。異なる意図のスライスを同じ委任へ混ぜない。
+- generator の逸脱は親が plan へ転記する。escalate が返ったら停止する。
 
-## フェーズ3: 機械ゲート — 正しいか
+完了条件: 全実装項目に実差分で確認済みの完了報告または解決済み escalate がある。
 
-- プロジェクトのテスト・lint コマンドを plan-implement 自身が実行する。サブエージェントの「全緑でした」報告だけでは進めない
-- **テストは選別して回す**: 修正ループ中の再実行は、変更の影響範囲のテスト（変更したファイル・モジュールに対応するもの）だけでよい。**全量スイートはゲートの完了条件としてだけ1回**回す — 検算の原則は保ったまま、全量実行を最後の1回に集約する（変更していない部分のテストを毎周道連れにしない）
-- 失敗したら `code-generator` に差し戻して修正させ、再実行する。12回試みても収束しなければ実装を止めてユーザーに報告する
-- テスト・lint が緑になったら `code-review` スキルで AI コードレビューを行い、指摘を解消してから次へ進む。挙動ゲートより先に置く理由: 指摘の修正でコードが変わりうるため、ユーザーの動作確認を後にしないと確認がやり直しになる
-  - 指摘の修正は `code-generator` に差し戻す（指摘の一覧を渡す。テストは不変のまま）。修正後はテスト・lint を再実行し、必要なら code-review も再実行する。この修正ループも12周で収束しなければ止めてユーザーに報告する
-  - 指摘が「テスト自体の欠陥」を指す場合は、実装で辻褄を合わせさせず、ユーザーに確認のうえ `test-generator` に差し戻す（期待値の保護 — 基準側の変更は人間の確認を経る）
-  - effort は **low を基本**とし、次のいずれかに該当する変更は **high** に上げる: アーキテクチャ・データモデル・公開インターフェースに触れる / plan の複数項目にまたがる大きな変更 / 共通基盤など影響が長く残る箇所
-  - 解消せず受け入れる指摘は理由を `## Deviations` に記録する（diff-review の昇格④⑤として人間の目に入る）
-- 変更が**対外境界・依存追加**（diff-review の昇格リスト②③）に触れる場合に限り、`security-review` スキルでセキュリティレビューも実行する。該当判定の正本は diff-review 側の昇格リスト — 同表が改訂されたらそちらに従う。該当しなければ「security-review 対象外」と進行状況に記録して飛ばす（毎回走らせない理由: 境界に触れない変更への網羅監査はコストだけ増えて指摘が出ない。該当する diff はどのみち例外ゲートで人間の目にも入る）
-  - 指摘の扱いは code-review と同じ: 修正は `code-generator` に差し戻してテスト・lint を再実行し、解消せず受け入れる指摘は理由を `## Deviations` に記録する
-- code-review（と、実行した場合は security-review）の指摘を畳んだら `simplify` スキルで品質整理（再利用・単純化・効率・抽象度）を適用する。code-review の後に置く理由: 正しさの修正を先に済ませないと、整理した形の上にまた修正が乗って二度手間になる（バグ探しは code-review、品質整理は simplify という役割分担どおり）
-- code-review / security-review / simplify で1行でもコードが変わったら、テスト・lint をもう一度実行して緑を確認する（ここも影響範囲の選別でよい）
-- **完了条件**: code-review と security-review（実行した場合）の指摘が解消済みまたは Deviations に記録済み、simplify の適用が済み、**最後のコード変更より後に全量の**テスト・lint が exit 0 で終わっている（フェーズ2のサブエージェント報告の使い回しは不可）
+## フェーズ3: 機械ゲート
 
-## フェーズ4: 挙動ゲート — 意図どおりか（diff より先）
+1. 親が影響範囲のテスト・lintを実行する。失敗は code-generator へ直列で差し戻し、12回で収束しなければ停止する。
+2. 緑になったら、フェーズ1 preflightで保持した `resolved_review_skill`（realpath済み絶対パス）だけを使い、Codex公式内部Skillを唯一の正本とする `review-agent` を起動する（defect-first）。フェーズ3で公式SKILLを別パスから再解決しない。
+   - 履歴なしの子を `model: "gpt-5.6-sol"`、`reasoning_effort: "xhigh"`、`fork_turns: "none"` で起動する。reviewer への委任プロンプトに渡すデータは次だけに限定する:
+     ```
+     CRYSTALLIZE_CODEX_ROLE=code-review
+     review_skill: <フェーズ1で保持したresolved_review_skillの絶対パス>
+     plan: <plan の絶対パス>
+     review_target_diff: <レビュー対象diff>
+     verification: <検証コマンド・exit status・結果>
+     output_contract: read-only / findings-first / actionable findings を P0-P3 順で返す。該当なしは `No findings.` とし、最後に overall assessment と material test gaps / residual risks を短く返す
+     ```
+   - reviewer は解決済みの公式 `SKILL.md` を全文読み、その契約（read-only、defect-first、P0-P3、`No findings.` を含む）を実行する。親の推論・ledger・未指定の背景は渡さない。
+   - preflight済み公式SKILLの読取、または履歴なし子の起動に失敗したら、汎用レビュープロンプトや同一コンテキストへフォールバックせず、コードレビュー工程を未完了として停止・報告する。
+3. 指摘修正は code-generator へ差し戻し、親が検証結果を更新して再検証する。変更後は同じ公式 review-agent 委任を再実行する。テスト自体の欠陥はユーザー確認後にだけ test-generator へ戻す。解消しない指摘は理由を Deviations へ記録する。
+4. diff-review の昇格条件②対外境界または③依存追加に触れる場合だけ、別の `gpt-5.6-sol` / `xhigh` の履歴なし read-only security reviewer を起動する。該当しなければ「security review対象外」と記録する。
+5. 最後に別の `gpt-5.6-luna` / `max` の履歴なし変更サブエージェントへ、挙動・公開契約・テストを変えず、重複・不要な抽象化・複雑さだけを整理する品質整理タスクを渡す。変更があれば親が再検証する。
+6. 最後のコード変更より後に全量のテスト・lintを親が1回実行する。
 
-- spec の「実機確認に委ねる基準」、「(モック確定・実機で再確認)」印の要件、受け入れ基準のいずれにも見た目や UI 操作の項目が無ければ、「UI確認対象なし」と進行状況に記録してフェーズ5へ進む
-- あれば `run` スキルでアプリ/モックを起動し（ブラウザ対象で Orca が使える環境なら `orca-cli` でもよい。無ければユーザーにブラウザで開いてもらう）、チェックリストを**ユーザーに提示し、ユーザー自身に触ってもらう**。並び順は spec の「実機確認に委ねる基準」と「(モック確定・実機で再確認)」印の付いた要件が先頭、続けて受け入れ基準の操作パス — 印の付いた項目はモックの反応で仮決めした箇所で、実機で覆る確率が最も高い。`covers:` に `実機:` の項目があれば、実機確認に委ねる基準とモック確定印の要件はそこに挙がったものだけを載せる。`docs/crystallize/specs/<slug>/mock.html`（`spec` スキルの反応用モック。spec 配下なので消えずに残っている）があれば見比べの基準として使ってよい
-- 「ここ違うな」が出たら修正の性質でレーンを分ける。**どのレーンでも、このループが収束するまで diff-review は呼ばない**
-  - **微修正レーン** (テストで固定した挙動の契約を変えない修正 — 見た目・文言・定数調整など): TDD の重機を回さない (`test-generator` を呼ばない — UI・見た目は TDD 対象外の原則の再適用)。指摘はユーザーが一通り触り終えるまで**束ね、まとめて1回** `code-generator` に委任する。色・余白・文言レベルの1〜2行の即時調整でユーザーが画面の前で待っている場合に限り、plan-implement が直接編集してよい (委任の固定費が作業を上回るため — 委任原則の明文化された例外)。再検証は lint + 影響範囲のテストのみで、code-review は再実行しない (差分の全量は最後に例外ゲートを通る)。hot reload など実行中のアプリを保ったまま反映できる手段があれば使い、その場で再確認する
-  - **正規レーン** (実装が spec と食い違っていた修正 — 要件は正しいが挙動がそれに届いていないもの): 従来どおりフェーズ2に差し戻し、修正後は影響範囲のフェーズ3だけ再実行してからこのチェックリストに戻る
-  - **仕様改訂レーン** (ユーザーが要件そのものを変えた修正 — 「やっぱりこうしたい」): 実装は spec のとおりだが、触ってみて spec の側が間違っていたと分かった場合。手順は次の4つ
-    1. **「これは仕様の変更として spec に記録します: <変更点1行>」と1回だけ宣言する**。以後、同じ点について「仕様と違いますがよいですか」と聞き直さない — 反復は本人が決めたことの許可をもう一度求める作業で、ユーザーが仕様の変更を言い出しにくくなる
-    2. spec の要件と受け入れ基準の該当行を書き換え、改訂履歴に1行（日付 / 項目 / 変更 / 理由 / 場面は「挙動ゲート」）を追加する。書き換えた要件行にも否定形の帰結を併記する（spec の要件節の作法を維持する）。spec の無い旧形式の plan なら、同じ内容を plan の `## Deviations` に「仕様改訂」と明記して書く（記録先が無いことを理由に記録を省かない）
-       - **分割 plan（`covers:` 行がある）で受け入れ基準の行が増減したときは、同じ手順の中で `covers:` 行も直す** — 追加した AC は、その挙動を実装するのがこの plan なら自分の `covers:` に足す。他の plan の担当なら `plans/` に残っているその plan の `covers:` に足し、残っていない（コミット済み）なら止めてユーザーに伝える（実装済みの範囲に後から要件を足すことになるため、新しい plan を立てるか次の plan に載せるかの判断が要る）。削除した AC は自分の `covers:` から外す。ここで直さないと、どの plan の担当でもない AC が生まれて誰も実装しないまま全 plan がコミットされる
-    3. 影響する受け入れ基準の行（AC-n）だけを `test-generator` に差し戻す — 「期待値の変更は人間の確認を経る」規則のその確認が、1 の宣言にあたる
-    4. `code-generator` で実装 → 影響範囲だけフェーズ3を再実行 → このチェックリストに戻る
-    - モック確定印の要件など、受け入れ基準の行に影響しない見た目だけの変更でも 1・2 は行う（spec を現実に合わせるのが目的で、記録を省くと次の実装で同じ議論をやり直す）。3 は差し戻す行が無いので省き、コード側は微修正レーンの作法でよい
-- **完了条件**: チェックリストの全項目（実機確認に委ねる基準・モック確定印の要件・受け入れ基準の操作パス）について、ユーザー自身の言葉で「意図通り」の確認が取れている（UI確認対象なしの記録で代えた場合も可）
+完了条件: コードレビュー必須、条件該当時のsecurity review、品質整理、最後の全量test/lintが完了し、未解決指摘はDeviationsに記録済み。
 
-## フェーズ5: 例外ゲート — 危なくないか
+## フェーズ4: 挙動ゲート
 
-- フェーズ4収束後に初めて `diff-review` を Skill ツールで直接起動する（案内文にしない）: skill = `diff-review`, args = `plan: <plan のパス>`
-- ユーザーが既に別のレビュー手段（例: codex のレビューループ）を使うと明言している場合は起動を省いてよいが、レビューそのものの省略は認めない
-- **昇格 hunk が0件のとき、diff-review は画面を作らず「全 M hunk 中、昇格 0 件 (①〜⑥該当なし)。N files +a -d (レビュー対象外 X hunk: reports/plans を除外)。go / no-go を」の1行だけを返す**（`M` は除外後の判定対象 hunk 数。除外が0件なら括弧ごと省く）。その1行を検算の数字ごとそのままユーザーに示して go / no-go を得る — 画面が無いのは省略ではなく「見せるべき例外がなかった」という結果で、レビュー未実施として扱わない
-- 昇格ありなら生成された画面を開いてもらい、決裁カードごとの「このリスクを受け入れるか」の判断として明示的な go / no-go を得る。no-go ならフェーズ2かフェーズ4へ差し戻す
-- **完了条件**: ユーザーの明示的な go が得られている（この go はフェーズ6のコミット同意を兼ねる）
+- spec の「実機確認に委ねる基準」、「(モック確定・実機で再確認)」印の要件、受け入れ基準のいずれにも見た目やUI操作が無ければ「UI確認対象なし」と記録する。
+- UI項目があれば、plan または既存資料にある起動コマンドを親が実行し、Codexのブラウザ・ファイル表示で優先項目と受け入れ基準の操作パスをユーザーへ提示する。起動コマンドが不明なら推測せず停止する。
+- ユーザーが触って出した微修正は束ねて code-generator へ渡す。色・余白・文言の1〜2行だけは親が直接直してよい。挙動契約が変わる修正はフェーズ2へ戻す。
+- ユーザーが「やっぱりこうしたい」と要件を変えたら仕様改訂レーンへ進む。まず「これは仕様の変更として spec に記録します: <変更点1行>」と一度宣言し、spec の要件・受け入れ基準・改訂履歴（場面は挙動ゲート）を更新する。分割 plan でACが増減したら該当 `covers:` 行も同時に更新する。影響するACだけを test-generator に戻し、code-generatorで実装して影響範囲の機械ゲートを再実行する。見た目だけの変更はテスト差し戻しを省く。
+- 変更後は必要な検証をやり直し、ユーザー自身の「意図どおり」が得られるまで diff-review を起動しない。
+
+完了条件: すべての操作パスでユーザー確認済み、またはUI確認対象なし。
+
+## フェーズ5: 例外ゲート
+
+- フェーズ4収束後、`gpt-5.6-sol` / `xhigh` の履歴なしの子へ `CRYSTALLIZE_CODEX_ROLE=diff-review`、`../diff-review/SKILL.md` の絶対パス、project_dir、`plan: <絶対パス>`、比較基点を渡す。
+- 親は生成HTML、昇格hunk数、全hunk検算を確認してCodex内で表示する。昇格0件なら diff-review のスキップ1行をそのまま示し、HTMLを作らない。別のレビュー手段を明示されてもレビュー自体は省略しない。
+- ユーザーから明示的な go / no-go を得る。no-go は適切なフェーズへ戻す。
+
+完了条件: ユーザーの明示的な go。この go はplan削除とコミットへの同意も兼ねる。
 
 ## フェーズ6: 確定
 
-- フェーズ5の go を受けて `plan-commit` を Skill ツールで直接起動する（案内文にしない）: skill = `plan-commit`, args = `<plan のパス>`
-- 仕様改訂レーンを通っていても、spec の改訂履歴をここで自分でコミットメッセージへ転記しない — 転記は `plan-commit` の手順3の担当（転記の担い手を1箇所にしないと、二重に入るか両方が相手に任せて落ちる）
-- `plan-commit` が返す `git log -1 --stat` の要点をそのまま報告する。スライスごとの経過は語り直さない
-- **完了条件**: `plan-commit` が完了し、plan ファイルと plan と同名のディレクトリ（`docs/crystallize/plans/<plan の basename>/`）が削除されている（spec は残る）
+- 親が `../plan-commit/SKILL.md` を読み、同じコンテキストで plan の絶対パスとフェーズ5の go を入力として実行する。spec 改訂の転記は plan-commit に任せる。
+- `git log -1 --stat`、plan と同名作業フォルダの削除、無関係ファイルがコミットされていないことを確認して報告する。
 
 ## Gotchas
 
-- **下流スキルへの接続を「〜してください」の案内文に退化させない** — 過去の運用で、案内文止まりの下流スキル接続は高い頻度で実際には実行されず、即興レビューや手動 git 操作に置き換わることが繰り返し観測された。フェーズ5・6は必ず Skill ツールで直接起動する
+- 下流名を書くだけでは実行にならない。必ずSKILLの絶対パスを解決し、fresh subagentまたは同一コンテキスト実行を契約どおり完了する。
+- evaluator・generator・reviewerの子には親のledger、決着済み議論、推論過程を渡さない。
+- 子は同じfilesystemを共有する。変更系の子を並列起動しない。
